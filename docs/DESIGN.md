# Design: Fleet — Command Console for Parallel Claude Agents

Generated by /office-hours on 2026-04-15
Branch: main
Repo: edisonshen/spark
Status: APPROVED
Mode: Builder

## Problem Statement

A single operator running Claude Code across multiple concurrent projects (rainier, gift-finder, caching project, side projects) spends more time re-onboarding themselves to each agent than managing the work. Supervision capacity, not Claude throughput, is the ceiling on how much work one person can dispatch.

Existing tools solve pieces: Switchboard organizes repos by folder; Linear/GH track tasks; Claude Code runs a single agent. None expose the operator's multi-agent reality as a first-class surface, and none quantify "is this agent still worth talking to."

Fleet is an open-source command console that treats the one-person-operator as the bottleneck and optimizes everything around maximizing their parallel-supervision capacity. Slogan: **Everyone is a manager.**

## What Makes This Cool

**The parallelism reveal demo.** Open Fleet's TUI, see 4 agents across 4 repos working simultaneously — one coding, one blocked on your input, one handing off, one reviewing. The viewer watches one person operate a small company in real time. "You're running a team." The feeling sells the thesis.

**Agent health as a first-class metric.** Fleet treats each agent's context-window %, last-activity, and handoff-readiness as dashboard primitives. Green/yellow/red on the bridge. Nobody else quantifies agent health this way. This is the conceptual bet — and the thing other tools will copy.

## Constraints

- **Terminal-first.** TUI is the launch surface. Web dashboard is explicitly v2.
- **Single-binary distribution.** Homebrew + GitHub Releases. No runtime deps.
- **Big-bang launch.** All three pillars (fleet view + dispatch-agent + context-guard) ship together. No partial rollout.
- **Solo-operator scale.** v1 optimizes for 1-20 concurrent agents on one laptop. Multi-machine fleets are v2+.
- **Claude Code native.** Agents are Claude Code instances. Fleet wraps and watches them; doesn't replace them.

## Premises

All agreed by operator:

1. Supervision capacity, not Claude quality, is the bottleneck. Parallelism pays off if switching is free.
2. Agent health is measurable enough to be useful: context-window % + last-activity + blocked flag = strong enough signal.
3. TUI is the right launch surface. Web reach broader but wrong complexity for v1.
4. Handoff-before-degrade is real: context > 50% correlates with worse decisions enough to act on.
5. Big-bang launch is right for Fleet specifically — the thesis needs a complete demo to carry.
6. Target user exists and is reachable: OSS AI-builder community running multiple Claude Code projects.
7. Distribution: single Go binary, brew tap + GitHub Releases.

## Approaches Considered

### Approach A: Go binary, flat-file state (CHOSEN)

Single `fleet` Go binary. TUI via bubbletea. CLI via cobra. Agent spawn via `os/exec` into detached tmux sessions. State is flat files in `~/.fleet/` — markdown task files + per-agent JSON health files. TUI uses **fsnotify** to watch state files; polling at 1s is the fallback for platforms where fsnotify misbehaves. Context-guard ships as a Claude Code skill that writes health JSON the TUI reads.

Effort: M (6-8 weeks solo including Week 0 spike). Risk: Medium — the whole thesis depends on whether a Claude Code skill can introspect `context_pct` (see Feasibility Spike below).

### Approach B: Daemon + IPC, thin client TUI

`fleetd` background daemon; `fleet` binary is a thin client over Unix socket. Event-driven, push-based metrics. Paves the way to a future web dashboard as another client.

Effort: XL (10-12 weeks). Ideal architecture but two months of daemon plumbing most v1 users never see. Deferred to v2.

### Approach C: Git-as-state, agents-as-PRs

Agent actions commit to a `fleet/tasks` branch. Dashboard = enriched `git log`. Handoffs = PRs. Multi-machine for free.

Memorable, but repo pollution and opinionated review workflow will lose more users than the novelty wins. Rejected.

## Recommended Approach

**Approach A**, composed of three shippable components inside one repo:

**1. `fleet` — Go binary (bubbletea + cobra)**

Commands:
- `fleet` or `fleet tui` — open the dashboard
- `fleet chat <project>` — spawn a **planner session** (Claude + `fleet-planner` skill) for project-level planning. Operator uses `/office-hours`, `/plan-eng-review`, etc., then `/fleet-sync` to propose tasks.
- `fleet plan <project>/<task>` — spawn a plan-mode agent; writes the task's `## Plan` section and stops
- `fleet plan <project>/<task> --redo` — re-plan when the existing plan is stale
- `fleet dispatch <project>/<task>` — spawn an execute-mode agent; requires `## Plan` unless `--skip-plan`
- `fleet dispatch <project>` — dispatch on highest-priority `planned` task
- `fleet attach <agent-id>` — re-attach to a running agent's tmux session (full interactive)
- `fleet peek <agent-id>` — read-only status: health, recent actions, last output (non-disruptive)
- `fleet msg <agent-id> "text"` — send async message to agent's inbox
- `fleet broadcast <project> "text"` — message all active agents in a project
- `fleet status` — non-TUI JSON/plain summary for scripting
- `fleet handoff <agent-id>` — force-save handoff doc and restart fresh agent
- `fleet projects add <path>` — register a project directory
- `fleet tasks add <project> "<title>"` — create a task file + add to manifest
- `fleet tasks unblock <project>/<task>` — clear an unhealthy task (A4)

**Task lifecycle (plan-first).**

```
  todo ── fleet plan ──▶ planning ──▶ planned ── fleet dispatch ──▶ doing ──▶ done
                                         │            │               │
                                         │            └─[cap]─▶ queued ──▶ doing
                                         │                            │
                                         │                            ├──▶ blocked
                                         │                            │
                                         │                            └──▶ unhealthy (A4)
                                         │
                                         └── fleet plan --redo ──▶ planning
```

- A new task lands in `todo`. `fleet plan` promotes it through `planning` (agent is writing) → `planned` (plan saved, awaiting operator review).
- `fleet dispatch` requires `planned`. Error on `todo`: `fleet dispatch: no plan for <task>. Run \`fleet plan <task>\` first, or pass --skip-plan to execute ad-hoc.`
- `--skip-plan` escape hatch covers trivial tasks (typo fix, version bump) where planning is overhead.
- `--redo` on `plan` is for stale plans — clears the old `## Plan` section and re-runs plan mode.
- The plan lives in the task file as the `## Plan` section, directly after `## Context`. Operator can edit it in `$EDITOR` before dispatching — same file the agent wrote it to.

**TUI layout (rough ASCII):**

```
┌─ Fleet ──────────────────────────────────── edisonshen ──┐
│ [p]rojects  [t]asks  [a]gents                  All ▾     │
├─────────────────────────────────────────────────────────┤
│ rainier (2 tasks, 1 active, 1 queued)                    │
│   ● a4  auth-token-refresh     ●31%  2s   doing  #4     │
│   ⏸     add-rate-limiting       —    —    queued #0     │
│                                                          │
│ gift-finder (1 task, 1 active)                           │
│   ● a2  rec-engine-v2          ●47%  4m   doing  #1     │
│                                                          │
│ caching (1 task, 1 active)                               │
│   ⏸ a3  redis-migration        ●21%  12m   blocked #2   │
│                                                          │
│ spark (1 task, 1 active)                                 │
│   ● a5  fleet-self-host        ●61%  14s  doing  #1     │
├─────────────────────────────────────────────────────────┤
│ 4 projects · 5 tasks · 4 agents active · 1 queued        │
│ [c]hat [p]lan [d]ispatch [a]ttach [h]andoff [n]ew [q]uit│
└─────────────────────────────────────────────────────────┘
```

TUI navigation. Action keys are context-sensitive: the key's effect
depends on whether a project row or a task/agent row is selected.

- **Default view:** all projects, grouped. Shows active agents + queued
  tasks. Header strip (`[p]rojects [t]asks [a]gents`) are view labels;
  the primary action keys are in the bottom strip.
- **`Enter`** — drill into the selected row (project → its tasks;
  task → detail pane; agent → health + recent actions).
- **`c`** (project row) — chat: spawn a planner session on the selected
  project. Attaches operator to a tmux Claude Code session with the
  `fleet-planner` skill loaded; `/fleet-sync` proposes tasks for
  operator approval.
- **`p`** (task row) — plan: spawn plan-mode agent on the selected
  task. Writes the task's `## Plan` section and stops.
- **`d`** (task row) — dispatch: spawn execute-mode agent on selected
  task (requires `planned` unless `--skip-plan`).
- **`a`** (agent row) — attach to the agent's tmux session (full
  interactive).
- **`h`** (agent row) — handoff: save doc + 3s grace + fresh successor.
- **`n`** — new: create a project (on empty dashboard or header) or a
  new task (on selected project row).
- **`e`** (task row) — edit task file in `$EDITOR`.
- **`q`** — quit.
- Alert-specific keys (context-sensitive in detail panes): **`[u]nblock`**
  for A4 unhealthy, **`[k]` acknowledge** for emergency-handoff banner,
  **`[l]` tail logs** from detail panes.
- `#N` column shows handoff count — higher number = task has seen more
  agents.

Health thresholds (single source of truth — used by colors and the
handoff trigger). **Threshold effects are mode-aware.**

Modes split into two families:

- **Doing modes** (`execute`, `fix`) — graceful handoff at 50% fires at
  the next `MILESTONE`; emergency kill-and-respawn at 70% if agent
  ignored the queued handoff.
- **Thinking modes** (`plan`, `review`) — reminder-only; operator
  decides when to handoff. Claude Code's own `/compact` at ~95% is the
  backstop.

| Band        | Doing (execute, fix)                                    | Thinking (plan, review)               |
|-------------|---------------------------------------------------------|---------------------------------------|
| Green       | < 50%, active ≤ 60s, not blocked                        | same                                  |
| Yellow      | **≥ 50% — handoff queued; fires at next `MILESTONE`**   | 50-70% — `⚡` reminder in TUI        |
| Red         | ≥ 70% — **hard kill-and-respawn** (safety net)          | ≥ 70% — `⚠` urgent reminder         |
| Backstop    | —                                                       | ~95% — Claude Code's own `/compact`   |

In doing modes, one token (`MILESTONE`) serves double duty: progress
signal in normal operation; exit trigger when `HANDOFF REQUESTED` has
been injected into the agent's context. Clean CLAUDE.md snippet, no
special-case handoff token.

Premise 4 ("act at 50%") holds exactly: 50% is the action threshold in
both mode families. Doing modes queue a graceful handoff at 50% (agent
finishes current milestone, then hands off); thinking modes fire a `⚡`
reminder. Hard kill-and-respawn sits at 70% as the safety net for doing
modes — 20% runway from graceful queue to emergency kill is the
design's bet that any bounded work unit wraps in under 20% of context.
Same 50/70 numbers across mode families; only the enforcement differs.

Note: `[r]eview` (as in "review queues — operator's TODO review") was dropped from v1 keybinds. The post-execute *code* review loop (Phase 4 §4.7 in FLOW.md) is a different concept, orchestrated by Fleet itself and requires no operator keybind.

**2. Data Model — Operator → Projects → Tasks → Agents**

The core hierarchy:

```
Operator (1 human)
  └── Project (many — rainier, gift-finder, caching, spark)
       └── Task (many per project — auth-fix, rec-engine, redis-migration)
            └── Agent chain (many per task, sequential — a1 → handoff → a4 → a7)
```

Fleet tracks this hierarchy across two locations:

- **Central state** (`~/.fleet/`) — project manifests, agent health, handoffs, progress logs. Fleet owns this.
- **Per-project task files** (`<repo>/tasks/<slug>.md`) — detailed task context that lives with the code. Agents read and append to these.

**2a. Project manifest (`~/.fleet/projects/<name>.yaml`)**

Each registered project gets a manifest. This is the source of truth for "what work exists" and enables auto-spawn.

```yaml
name: rainier
repo: /Users/edison/projects/rainier
auto_spawn: true          # when agent hands off, auto-start fresh agent
max_concurrent_agents: 2  # cap parallel agents on this project
tasks:
  - id: auth-token-refresh
    status: doing           # todo | planning | planned | queued | doing | blocked | unhealthy | done
    priority: 1
    current_agent: a4
    handoff_count: 3
    created: 2026-04-15
    task_file: tasks/auth-token-refresh.md   # path relative to repo root

  - id: add-rate-limiting
    status: todo
    priority: 2
    current_agent: null
    handoff_count: 0
    created: 2026-04-15
    task_file: tasks/add-rate-limiting.md

  - id: fix-logging-format
    status: done
    priority: 3
    current_agent: null
    handoff_count: 1
    created: 2026-04-14
    completed: 2026-04-15
    task_file: tasks/fix-logging-format.md
```

Fleet reads all project manifests on startup to build the TUI dashboard. Changes to manifests are watched via fsnotify.

**Auto-spawn flow:**
1. Agent a4 hits Yellow/Red threshold → `fleet-guard` triggers handoff → writes handoff doc.
2. Fleet reads the project manifest: `auto_spawn: true` for rainier.
3. Fleet checks `max_concurrent_agents`: rainier has 1 active (a4 departing), cap is 2 → clear to spawn.
4. Fleet writes `~/.fleet/queue/spawn-fresh.json` with `{task_id, project, from_handoff}`.
5. Fleet picks up the queue event, spawns fresh agent in tmux with handoff doc pre-loaded.
6. Updates manifest: `current_agent: a7`, `handoff_count: 4`.
7. If `auto_spawn: false` → TUI shows "Agent a4 handed off on auth-token-refresh. Spawn replacement? [y/n]" and waits.

**Task queue behavior:**
When `auto_spawn: true`, Fleet also picks up the *next task* if the current one finishes:
- Agent completes `auth-token-refresh` → marks `status: done` in manifest.
- Fleet checks: any `todo` tasks for rainier? `add-rate-limiting` is `priority: 2`, `status: todo`.
- Fleet auto-dispatches an agent on `add-rate-limiting` (respecting `max_concurrent_agents`). If the task has no `## Plan`, Fleet auto-runs `fleet plan` first, then pauses at `planned` for operator review before dispatching. `auto_plan_and_dispatch: true` in the manifest skips the review gate.
- Operator wakes up, checks TUI: "rainier: auth-fix done, rate-limiting in progress."

**CLI for managing the hierarchy:**
- `fleet projects add <path>` — register a project, create empty manifest
- `fleet projects list` — show all projects with task counts and active agents
- `fleet tasks add <project> "<title>"` — create a task file + add to manifest
- `fleet tasks list <project>` — show tasks for a project
- `fleet dispatch <project>/<task>` — spawn agent on a specific task
- `fleet dispatch <project>` — spawn agent on the highest-priority `todo` task

**2b. Task file (per-project, lives with the code)**

Lives in each project repo at `tasks/<slug>.md`:

```markdown
---
id: auth-token-refresh
project: rainier
status: doing
priority: 1
current_agent: a4
created: 2026-04-15
---

# Fix token refresh race condition

## Goal
Eliminate the race where concurrent goroutines both detect token expiry and both trigger a refresh, causing 401 cascades under load.

## Context
- auth/middleware.go:84 is the refresh call site
- Repro: run load test with 50 concurrent requests during token expiry window
- Related: issue #142

## Progress (agent-appended)
- 2026-04-15 10:00 [a1] — Reproduced locally, root cause identified
- 2026-04-15 12:15 [a1] — Wrote fix (sync.Once) + test, handoff at 68% context
- 2026-04-15 14:32 [a1] — Integration tests pass, handoff at 72% context
- 2026-04-15 14:33 [a4] — Picked up from handoff #3, reviewing test coverage
```

Agents append to `## Progress` as they work, prefixed with timestamp and agent ID. Fleet reads frontmatter for status/assignee. The task file frontmatter and the project manifest stay in sync — Fleet updates both atomically when status changes.

**Sync rule:** Project manifest is the leader. If manifest says `status: doing` but task file says `status: todo`, Fleet overwrites the task file to match. This prevents split-brain when an agent modifies the task file but crashes before updating the manifest.

**3. Context-guard Claude Code skill (`fleet-guard`)**

A Claude Code skill installed at `~/.claude/skills/fleet-guard/SKILL.md`. Runs via the `PostResponse` hook (the hook that fires after every agent turn; Week 0 spike verifies this hook exposes context metrics — see Feasibility Spike). On each response:

- Reads `context_pct` from Claude Code's hook payload if available. If the hook does not expose token counts, falls back to a proxy: `messages_since_spawn × avg_tokens_per_turn / model_context_limit`. The spike determines which mode is used; both write the same health JSON shape.
- Writes `~/.fleet/agents/<id>.json` with `{id, pid, tmux_session, task_id, context_pct, context_source: "hook"|"proxy", last_activity_ts, blocked, needs_input}`.
- At context_pct ≥ 50% (Yellow, doing modes): injects `HANDOFF REQUESTED` into the agent's next turn. Agent wraps the current bounded work unit and emits `MILESTONE`; fleet-guard writes the handoff doc and enters the 3s grace window. Thinking modes (plan, review) get a `⚡` reminder only — operator decides.
- At context_pct ≥ 70% (Red, doing modes only): emergency kill-and-respawn without grace. Thinking modes get a `⚠` reminder — still no enforcement; Claude Code's own `/compact` at ~95% is the backstop.
- "Signals Fleet" = writes to `~/.fleet/queue/` directory. Fleet watches this via fsnotify. No sockets, no IPC beyond the filesystem.

Skill lives in this same repo under `skills/fleet-guard/` and is copied into `~/.claude/skills/` by `fleet init`.

**Spawn & Lifecycle**

- **tmux session naming:** `fleet-<agent_id>` where `agent_id` is a short random slug (`a1b2`). One Claude Code process per tmux session, one session per agent.
- **Skill injection:** `fleet dispatch` writes a per-agent `CLAUDE.md` snippet into a temp dir that forces `fleet-guard` to load, then launches `claude` with cwd set to the project repo and env var `FLEET_AGENT_ID=<id>` so the skill knows which health file to write.
- **Liveness probe:** Fleet checks tmux session existence + PID every 5s. If tmux session gone but health file still present → mark agent `crashed`, move health file to `~/.fleet/agents/archive/`. User can see crashed agents in the TUI under a separate "Recent" section.
- **Stale detection:** If health file's `last_activity_ts` is older than 30 minutes AND tmux session is gone, auto-archive. If tmux session is alive but `last_activity_ts` is old, show as idle (not crashed) — agent is just waiting on user input.
- **Reconciliation:** Task file frontmatter `assigned: <agent_id>` is the source of truth for *who owns* the task; `~/.fleet/agents/<id>.json` is the source of truth for *agent health*. On conflict (task file says assigned to `a1` but no health file exists), Fleet shows the task as "assigned but agent missing" and offers `fleet recover <task>`. On crash, Fleet clears the `assigned` field from the task file after archival.
- **Restart on handoff (no race, no mid-response kill):** Flow is cooperative, not coercive:
  1. `fleet-guard` PostResponse hook detects Yellow threshold (50%, doing modes) OR operator-initiated handoff (`[h]` key). Injects `HANDOFF REQUESTED` into the agent's context. For emergency (70%, doing modes), skips the injection and goes straight to step 2.
  2. Agent finishes the current bounded work unit (commit, test pass, sub-step), emits `MILESTONE` on its own line. Skill greps the tmux pane capture for `MILESTONE` while `HANDOFF REQUESTED` is pending. On match, skill writes the handoff doc to `~/.fleet/handoffs/<id>-<ts>.md`. Only after fsync succeeds does it write the trigger `~/.fleet/queue/handoff-<id>.json` with `{handoff_path, agent_id, task_id}`.
  3. Fleet's TUI watches `~/.fleet/queue/` via fsnotify. On `handoff-<id>.json`, it shows the agent row as `handoff: saving...` (amber) → `handoff: 3s` countdown. `[c]` during the countdown cancels (doc moves to `~/.fleet/handoffs/archive/.cancelled-<ts>.md`, agent resumes).
  4. After the 3s grace, Fleet sends `/exit` to the Claude session. Emergency handoffs skip the grace entirely — immediate `/exit` on the 70% trigger.
  5. Fleet sends tmux `send-keys` with `/exit` to the Claude session — a graceful shutdown, not SIGKILL. 3-second grace window in the TUI where operator can press `c` to cancel before the fresh spawn starts.
  6. Fleet spawns the fresh agent via `fleet dispatch --from-handoff <path>`. This is mechanically a regular spawn with two extras: (a) a temp `CLAUDE.md` snippet added via env var `FLEET_EXTRA_CLAUDE_MD=<tmpfile>` that says *"You are resuming work. Read the handoff doc at <path> first before responding."*, and (b) the first user-message prefill is piped in via `claude --initial-prompt "Read and continue from <handoff_path>"`. No mid-response termination, no lost state.
- **Agent ID changes on handoff; task `assigned` field updates atomically.** TUI shows the handoff as a single transition, not two separate agents, to preserve the "fleet never dies" narrative.

**Feasibility Spike (Week 0 — GATING)**

The entire product rests on whether a Claude Code skill can read the host's `context_pct`. The spike must answer three questions before any `fleet` code ships:

1. **Availability.** Does Claude Code's hook/skill API expose current token usage or context-window %? Answered by reading Claude Code's skill/hook reference and a 1-hour experimentation session. Pass if the data is exposed; fail if it is not.
2. **Latency.** If exposed, is `context_pct` available within 500ms of the PostResponse hook firing? Pass if ≤500ms; fail if >500ms or unreliable. (The TUI polls at 1s; anything slower than 500ms means stale reads.)
3. **Proxy accuracy.** If (1) or (2) fails, measure proxy accuracy over 5 real Claude Code sessions that include tool use, file reads, and long-context tasks. Proxy formula: `tokens_estimate = system_prompt_tokens + sum(message_tokens) + sum(tool_result_tokens) + sum(file_read_tokens)`, measured by scraping the tmux pane capture + estimating tokens via tiktoken at turn boundaries. Pass if proxy stays within ±20% of ground truth (`/context` output). Pass at ±10% is ideal but unrealistic given file reads dominate; ±20% still supports meaningful Yellow/Red thresholds. Fail if proxy is worse than ±20%.

Spike result decides path:
- **(1) PASS and (2) PASS:** proceed as specified, use hook data directly.
- **(1) or (2) FAIL but (3) PASS:** proceed with proxy mode. Add `context_source: "proxy"` to health JSON so TUI can show "~73%" vs "73%" disambiguation. README notes health is estimated.
- **All three FAIL:** pivot to narrower v1 — drop automatic handoff, make handoff entirely operator-triggered (they read `/context` themselves, hit `h` in TUI), reframe the thesis from "self-healing fleet" to "parallelism dashboard with one-key handoff." Re-scope before building.

The spike's deliverable is a 1-page decision doc committed to the repo at `docs/SPIKE-context-pct.md` so contributors see the reasoning.

**State directory:**
```
~/.fleet/
├── config.yaml                      # global preferences, defaults, engines map
├── projects/
│   ├── rainier.yaml                 # project manifest: tasks, auto_spawn, max_agents
│   ├── gift-finder.yaml
│   └── caching.yaml
├── agents/
│   ├── a1.json                      # live health, pid, tmux session
│   ├── a4.json
│   └── archive/                     # crashed/handed-off agents (7-day retention)
├── queue/
│   ├── handoff-a1.json              # trigger: skill → Fleet (handoff requested)
│   └── spawn-fresh-a2.json          # trigger: Fleet spawns replacement
├── inbox/
│   ├── a3.md                        # pending message from operator → agent
│   └── archive/                     # delivered messages (7-day retention)
├── handoffs/
│   └── a1-20260415-143200.md        # saved handoff docs, chained via frontmatter
├── progress/
│   └── auth-token-refresh.jsonl     # per-task event log (feeds TUI dashboard)
└── logs/
    └── a1-20260415.log              # tmux pane capture
```

**`config.yaml` engines map.** `fleet dispatch` looks up the spawn command
by engine name rather than inlining `claude` in source. v1 ships one entry:

```yaml
engines:
  claude-code:
    cmd: claude
    initial_prompt_flag: --initial-prompt
```

v1 only writes and reads `claude-code`. The map exists from day one so v1.1
can add a `codex:` entry (or whatever second engine wins) without touching
the dispatch code path or migrating the manifest. Per-agent engine is
recorded in `agents/<id>.json:engine`; per-project default in
`projects/<name>.yaml:engine`. See `docs/DECISIONS.md` 2026-04-26 entry
"v1.1 engine adapter — minimal v1 hooks" for the full constraint.

## Reliability Invariants

Fleet's filesystem-state contract. Implementers consult `docs/STATE.md` for
the full detail (schemas, shell/Go patterns, examples). The rules that must
hold at all times:

**Scope.** v1 is single-machine, single-operator. No cross-machine sync, no
network protocols. Multi-machine is v2+.

**Architecture posture.** Fleet is a supervisor, not a host. It watches
Claude Code instances and orchestrates their lifecycle via files + tmux. It
does NOT edit running agents' conversation history, make LLM calls on their
behalf, or reach inside Claude Code's process memory. Compression is the
host's job (Claude Code's own `/compact`); Fleet's lever is kill-and-respawn
with a handoff doc.

**A1 — Atomic file publish.** Every state file is published via `.tmp` +
`rename(2)`. Never truncate-and-write-in-place. Durable events (handoff
docs) additionally `fsync` file data and parent directory before rename.
Startup runs a reconcile pass that archives orphaned agents and discards
stale queue triggers.

**A2 — Concurrent CLI.** Per-project `flock(2)` on
`~/.fleet/projects/.locks/<name>.lock` during any dispatch/mutation critical
section. Non-mutating reads are lock-free.

**A3 — Handoff signal.** Fleet watches `~/.fleet/handoffs/` via fsnotify.
The atomic rename of the handoff doc IS the completion signal. No separate
sentinel file, no tmux pane grep. Control decisions are never derived from
Claude's free-form output.

**A4 — Auto-spawn rate limit.** When `auto_spawn: true`: max 3 spawns/task
per rolling hour, min 30s between same-task spawns. On budget exhaustion,
task marked `status: unhealthy` in the manifest and operator must clear it
with `fleet tasks unblock` before auto-spawn resumes.

**A5 — Schema versioning.** Every JSON shape carries `schema_version`.
`fleet` binary embeds the versions it knows and checks the skill's declared
version at startup: MAJOR mismatch refuses to run (`fleet init` needed);
MINOR warns. Migration functions from v1 are scaffolded day-one.

**F1 — Depth limit.** Supervised agents cannot dispatch children. `fleet
dispatch` (and `fleet plan`) refuse when `FLEET_AGENT_ID` env is set.
Fleet's thesis is horizontal parallelism, not hierarchy.

**F2 — Supervised-agent guardrails.** Two layers. (1) The fleet-guard
skill injects a CLAUDE.md guidance block instructing the agent not to run
mutating `fleet` subcommands. (2) The `fleet` binary, when it detects
`FLEET_AGENT_ID` is set, allowlists only read-only subcommands (`status`,
`peek`, `version`, `--help`) and refuses mutations with a clear error.

**Derivation note on the 50%/70% thresholds (Premise 4).** Independent
evidence from Hermes Agent (`ContextCompressor` auto-compacts at 50%) and
OpenClaw (auto-compaction triggers at similar-order thresholds) validates
Fleet's Yellow threshold at 50%. Red at 70% is Fleet's own design bet —
20% runway from graceful queue (50%) to emergency kill (70%) accommodates
a bounded work unit wrapping up. They are not guesses.

See `docs/STATE.md` for the full schemas, shell patterns, and crash-
recovery details.

## TUI Alert Surface

The operator-attention states above (A4 unhealthy, blocked agents, version
warnings, needs-input) render in a unified alerts surface. Three layers:

**1. Sticky alerts banner** — one-line strip directly under the title row,
shown only when at least one alert exists. Four severity-ordered categories,
each with a distinct glyph and color:

```
┌─ Fleet ─────────────────────────────── edisonshen ──┐
│ ⚠ 2 unhealthy  ⏸ 1 blocked  ✏ 1 needs input         │
├─────────────────────────────────────────────────────┤
```

- `⚠ N unhealthy` — red. A4 auto-spawn budget exhausted.
- `⏸ N blocked` — amber. Agent JSON has `blocked: true`.
- `⚡ N warning` — yellow. A5 MINOR version mismatch, schema drift notices.
- `✏ N needs input` — cyan. Agent JSON has `needs_input: true`.

Order is fixed (severity high → low). Zero-count categories are omitted.
If every count is zero the banner row disappears entirely.

**2. Inline row marker** — the leading glyph on each task/agent row
matches the alert category firing for that row. Replaces the default
`●`/`○` status dot. Example:

```
│   ⚠     add-rate-limiting    3×/1h  UNHEALTHY        │
│   ⏸ a3  redis-migration      ●21%  12m  blocked  #2  │
│   ✏ a5  session-repro        ●44%   1m  ask → yes?   │
```

**3. Detail drill-in** — pressing `Enter` or the alert's action key
(e.g. `u` for unhealthy) on a flagged row opens a detail pane. For A4:

```
┌─ Detail: rainier/add-rate-limiting ───────────────┐
│ ⚠ UNHEALTHY — auto-spawn paused                   │
│                                                   │
│ Budget:  3 of 3 spawns in last hour               │
│ Crashes: all 3 exited in <5s                      │
│          (health JSON never written)              │
│                                                   │
│ Next auto-spawn: paused until operator clears     │
│                                                   │
│ [u] unblock   [l] tail logs   [Esc] back          │
└───────────────────────────────────────────────────┘
```

Detail pane content rules:
- Show the budget state (`N of M spawns in last hour`) from manifest
  `spawn_history`.
- Dedupe crashes by signal: "all 3 exited in <5s" when identical, or
  "2× exited in <5s · 1× tmux session not found" when different. No
  individual timestamps — they don't help the operator decide.
- `[u] unblock` is mandatory in v1 (keyboard equivalent of
  `fleet tasks unblock <project>/<task>`).
- `[l] tail logs` is mandatory in v1 — opens the tmux pane capture log
  (`~/.fleet/logs/<id>-<date>.log`) in a pager. Bounded to last 200 lines
  on open.
- `[Esc]` returns to the dashboard.

Detail panes for `⏸ blocked`, `⚡ warning`, and `✏ needs input` follow the
same three-part shape (state summary, relevant data from the schema, action
keys) but with category-appropriate actions.

### Error and glyph conventions

Scoped to what the reliability invariants above (A4, A5, F1, F2) need.
Broader CLI/TUI conventions are deferred to TUI implementation (Weeks 2-3).

**Error message format.** stderr for errors, stdout reserved for
machine-readable output (`fleet status --json`).

- Pre-dispatch errors (version checks, env guards): `fleet: <message>`
- Subcommand errors (bad args, runtime failures): `fleet <subcmd>: <message>`

**Exit codes.**

| Code | Meaning                                    | Examples              |
|------|--------------------------------------------|-----------------------|
| 0    | success                                    |                       |
| 1    | usage error                                | bad flags, unknown subcmd |
| 2    | policy refusal                             | F1, F2, unblock needed |
| 3    | version mismatch (MAJOR, startup-fatal)    | A5 MAJOR              |
| 4    | state error                                | manifest missing, flock timeout |

**Glyph vocabulary** (used by banner, inline row markers, detail panes):

| Glyph | Meaning              | Color   | Source field                       |
|-------|----------------------|---------|------------------------------------|
| `●`   | active agent         | green   | `agents/<id>.json` live, context < 50% |
| `○`   | todo task            | dim     | manifest `tasks[].status == todo`  |
| `⚠`   | unhealthy task       | red     | manifest `tasks[].status == unhealthy` |
| `⏸`   | blocked / queued     | amber   | `agents/<id>.json blocked == true` OR `tasks[].status == queued` (status column disambiguates) |
| `⚡`   | warning (non-block)  | yellow  | runtime (A5 MINOR, per-file skip)  |
| `✏`   | needs operator input | cyan    | `agents/<id>.json needs_input == true` |

When a row has an alert firing, the alert glyph replaces the default
`●`/`○` status dot. The banner uses the same glyphs with counts.

**Accessibility.**

- `NO_COLOR=1` respected: colors strip, glyphs remain (Unicode carries
  severity even without color — meets WCAG-style non-color-only rule).
- `FLEET_ASCII=1` for terminals without reliable Unicode rendering:
  ASCII fallback (`!` unhealthy, `-` blocked, `*` warning, `?` needs-input).
- Minimum terminal size: 80×24. Below that the TUI refuses to start with
  `fleet: terminal too small (got <w>×<h>, need 80×24)`. Non-TUI
  subcommands (`status`, `peek`, `dispatch`, ...) have no size requirement.
- All actions reachable by keyboard. No mouse-only paths.

## Operator-Agent Communication

A manager doesn't attach to every employee's desk to check status. They peek at dashboards, send Slack messages, and only sit down when it's a real conversation. Fleet mirrors this with three communication modes:

### 1. Peek (read-only, non-disruptive)

See what an agent is doing right now without interrupting it. Like looking over someone's shoulder.

**TUI:** Select an agent row, press `Enter` to drill into the agent detail view:

```
┌─ Agent a3 ── caching/redis-migration ── ●21% ── blocked ⚠ ──┐
│                                                               │
│ BLOCKED: "Need Redis 7.x connection string for staging env.   │
│  Checked .env and vault — neither has it. Who has access?"    │
│                                                               │
│ Last 5 actions:                                               │
│  14:02  Read docker-compose.yaml                              │
│  14:03  Grep for REDIS_URL across repo                        │
│  14:04  Read .env.example                                     │
│  14:05  Attempted vault read — permission denied              │
│  14:06  Set blocked flag with reason above                    │
│                                                               │
│ Files modified this session:                                  │
│  migrations/003_redis7.go  (new)                              │
│  config/redis.go           (modified)                         │
│                                                               │
│ Context: ●21% (31k/150k tokens) · Active 12m · Handoff #2    │
├───────────────────────────────────────────────────────────────┤
│ [m]essage  [a]ttach  [h]andoff  [Esc] back                   │
└───────────────────────────────────────────────────────────────┘
```

**CLI:** `fleet peek <id>` — prints the same info to stdout (scriptable, works from any terminal).

**How it works:** Fleet reads from three sources:
- `~/.fleet/agents/<id>.json` — health, blocked flag, blocked_reason
- `~/.fleet/progress/<task-id>.jsonl` — recent actions (last 5-10 events)
- tmux pane capture (`tmux capture-pane -t fleet-<id> -p | tail -20`) — raw last 20 lines of agent output

No interaction with the agent. Pure read. Agent doesn't know you peeked.

### 2. Message (async, non-disruptive)

Send information to an agent without attaching to its session. Like a Slack message it reads on its next turn.

**TUI:** From agent detail view, press `m`, type your message, press Enter:

```
Message to a3: "Staging Redis connection string is redis://staging-redis.internal:6379/0. It's not in vault yet, hardcode for now."
```

**CLI:** `fleet msg <id> "your message here"`

**How it works:**
1. Fleet writes the message to `~/.fleet/inbox/<agent-id>.md`:
   ```markdown
   ---
   from: operator
   timestamp: 2026-04-15T14:10:00Z
   ---
   Staging Redis connection string is redis://staging-redis.internal:6379/0.
   It's not in vault yet, hardcode for now.
   ```
2. `fleet-guard` checks `~/.fleet/inbox/<agent-id>.md` on every PostResponse hook.
3. If inbox has content, skill injects it into Claude's context on the next turn: *"Message from operator: <content>"*
4. After injection, skill moves the inbox file to `~/.fleet/inbox/archive/<agent-id>-<ts>.md`.
5. Agent processes the message, clears its `blocked` flag if applicable, and continues working.
6. Fleet TUI shows: `a3: unblocked by operator message (14:10)` in the activity feed.

**Auto-unblock:** If an agent is `blocked` and receives an inbox message, `fleet-guard` automatically clears the `blocked` flag and sets `needs_input: false` in the health JSON. The operator message is assumed to be the unblock. If it wasn't sufficient, the agent will re-block and explain why.

### 3. Attach (full interactive, disruptive)

Take over the session. Like walking up to someone's desk and pair-programming.

**TUI:** Press `a` on a selected agent. **CLI:** `fleet attach <id>`

**How it works:** `tmux attach-session -t fleet-<id>`. You are now in the Claude Code session directly. Type normally. The agent sees your input as regular user messages.

**When to use:** Only when peek + message aren't enough — the agent is confused, you need to pair on something complex, or you want to steer the approach directly.

**Detach:** Standard tmux detach (`Ctrl-b d`). Agent continues working autonomously.

### 4. Broadcast (message all agents in a project)

**CLI:** `fleet broadcast <project> "message"` — writes to every active agent's inbox in that project.

**Use case:** "All rainier agents: staging is down until 3pm, don't run integration tests." Every active agent on rainier picks this up on their next turn.

### Agent-side: blocked_reason and needs_input

The health JSON (`~/.fleet/agents/<id>.json`) now includes structured block info:

```json
{
  "id": "a3",
  "task_id": "redis-migration",
  "project": "caching",
  "context_pct": 21,
  "last_activity_ts": "2026-04-15T14:06:00Z",
  "blocked": true,
  "blocked_reason": "Need Redis 7.x connection string for staging env. Checked .env and vault — neither has it.",
  "blocked_since": "2026-04-15T14:06:00Z",
  "needs_input": true,
  "inbox_pending": false
}
```

`fleet-guard` teaches agents to set `blocked: true` and `blocked_reason: "<specific question>"` whenever they can't proceed. The skill's prompt injection includes:

> When you cannot proceed without information from the operator, write a specific question as the blocked_reason. Don't say "I'm stuck." Say exactly what you need: the credential, the decision, the file path.

This ensures the TUI's peek view always shows *why* an agent is blocked in plain language.

### Status check without peeking

For a quick status scan across all agents, the TUI default view already shows health + last activity. But for a deeper periodic check, the TUI auto-surfaces agents that need attention:

**Attention queue** (top of TUI when items exist):
```
⚠ NEEDS YOUR ATTENTION:
  a3  caching/redis-migration   blocked 8m — "Need Redis staging conn string"
  a2  gift-finder/rec-engine    yellow 47% — handoff recommended
```

Only `blocked` agents and `yellow/red` health agents appear here. Green agents stay quiet — you don't need to check on productive employees.

## Context Handoff System (Three-Tier Memory)

Fleet's handoff system is what separates it from every existing tool. Existing handoff solutions (thepushkarp/handoff, Sonovore/claude-code-handoff, tzu-hsun/recap) are all single-agent, human-triggered. Fleet's handoffs are **automatic, structured, machine-readable, and multi-agent aware**.

### Prior Art Studied

- **thepushkarp/handoff**: PreCompact hook writes `HANDOFF.md` with timestamps. Blocks agent from stopping until fields are filled. Best existing single-agent approach.
- **Sonovore/claude-code-handoff**: Writes `.claude/context.md` + task files. Auto-loads on session start.
- **LangGraph checkpoints**: Full state snapshot at each step with "time travel". Gold standard for programmatic agents.
- **OpenAI Agents SDK**: Collapses prior conversation into single context message on agent handoff.
- **Memory Pointer Pattern**: Store large outputs externally, pass reference keys between sessions.
- **Practitioner consensus**: Compact at 60% not 90%. Summarize *completed* work, not attempted. "X is not yet implemented" > "Implement X next".

### Three Tiers

| Tier | File | Written by | Read by | Lifespan |
|------|------|-----------|---------|----------|
| **Task file** | `tasks/<slug>.md` | Human + agent appends to Progress | Every agent on this task | Permanent |
| **Handoff doc** | `~/.fleet/handoffs/<id>-<ts>.md` | `fleet-guard` skill | Next agent only | Chain (each links to previous) |
| **Progress log** | `~/.fleet/progress/<task-id>.jsonl` | `fleet-guard` on significant events | TUI dashboard (human only) | Permanent |

**Why three tiers, not one?** Different consumers need different granularity. The next Claude agent needs a concise "where we left off" (handoff doc), not a raw event log. The human operator watching the TUI needs a timeline of what happened across all agents (progress log), not a prose summary. The task file is the shared contract both read.

### Handoff Doc Structure

```markdown
---
agent_id: a1
task_id: auth-token-refresh
project: rainier
context_pct_at_handoff: 72
previous_handoff: ~/.fleet/handoffs/a1-20260415-140000.md
handoff_number: 3
timestamp: 2026-04-15T14:32:00Z
---

## Completed
- Reproduced token refresh race in auth/middleware.go:84
- Root cause: concurrent goroutines both check expiry, both refresh

## Key Decisions
- Chose sync.Once over mutex (simpler, sufficient for single-refresh)

## Files Modified
- auth/middleware.go — added sync.Once guard around refresh call
- auth/middleware_test.go — added concurrent refresh test (passes)

## Open Questions
- Should we add a retry on 401 in the HTTP client layer? Deferred.

## Next Steps (prioritized)
1. Run full integration test suite (not yet run)
2. Check if other services hit the same pattern (search for refreshToken)
3. Open PR if tests pass
```

Rules enforced by `fleet-guard`:
- **Completed** section describes finished work, not attempted work (steal from practitioner consensus).
- **Files Modified** includes paths — the next agent can verify the files exist and read them.
- **Next Steps** are prioritized and specific, not vague ("continue working on it").
- Agent is blocked from stopping (up to 3 retries) until all sections are filled (steal from thepushkarp/handoff).

### Handoff Chain

Each handoff doc references the previous via `previous_handoff` in frontmatter. Fleet can reconstruct the full history for any task by following the chain. The TUI "task detail" view shows the chain as a timeline:

```
Task: auth-token-refresh (rainier)
Status: doing — agent a4 active (●31%)

Handoff chain:
  #1  a1  04-15 10:00  →  Reproduced bug, identified root cause
  #2  a1  04-15 12:15  →  Wrote fix + test, sync.Once approach
  #3  a1  04-15 14:32  →  Tests pass locally, needs integration suite
  #4  a4  04-15 14:33  →  Active now (picked up from handoff #3)
```

**The next agent gets ONLY:** task file + latest handoff doc + one line: *"This is handoff #3 for this task. Prior handoffs exist at ~/.fleet/handoffs/ but the latest doc is sufficient."* Full chain is for human context, not agent context.

### Progress Log (JSONL)

Machine-readable event log for the TUI, not for Claude:

```json
{"ts":"2026-04-15T14:20:00Z","agent":"a1","event":"file_modified","path":"auth/middleware.go","summary":"added sync.Once guard"}
{"ts":"2026-04-15T14:25:00Z","agent":"a1","event":"test_run","result":"pass","files":["auth/middleware_test.go"]}
{"ts":"2026-04-15T14:32:00Z","agent":"a1","event":"handoff","reason":"context_yellow","handoff_number":3}
{"ts":"2026-04-15T14:33:00Z","agent":"a4","event":"spawn","from_handoff":"~/.fleet/handoffs/a1-20260415-143200.md"}
```

Feeds the TUI's aggregate views:
- **Agent row:** "a1: 2 file changes, 1 test run, handed off at 72%"
- **Morning standup:** "Overnight: 3 agents worked, 2 handoffs, 7 files modified, 0 errors"
- **Task detail:** full timeline of actions across all agents that touched this task

### Automatic Handoff Triggers

| Trigger | Source | Behavior |
|---------|--------|----------|
| Context ≥ 50% (Yellow, doing modes) | `fleet-guard` PostResponse | Inject `HANDOFF REQUESTED`; next `MILESTONE` triggers graceful handoff |
| Context ≥ 70% (Red, doing modes)   | `fleet-guard` PostResponse | Emergency kill-and-respawn; no grace window |
| Context ≥ 50% / ≥ 70% (thinking modes) | `fleet-guard` PostResponse | Reminder only (`⚡` / `⚠`) in TUI alerts banner |
| PreCompact hook | Claude Code auto-compaction | Emergency handoff — fire before compaction, save state even if context measurement is unavailable |
| Human-triggered | TUI `h` key or `fleet handoff <id>` | Immediate handoff, same flow as Red |
| Idle timeout (>30min) | Fleet liveness probe | Mark agent stale, prompt operator: handoff or kill? |

The PreCompact trigger is critical insurance: even if the context-% measurement is imperfect (see Feasibility Spike), Fleet catches the compaction event and saves state. This is the safety net that makes the whole system resilient.

### Context Recovery on Spawn

When Fleet spawns a fresh agent (via `fleet dispatch` or handoff restart):

1. Fleet writes a temp CLAUDE.md snippet injected via `FLEET_EXTRA_CLAUDE_MD` env var:
   ```
   You are resuming work on a task managed by Fleet.
   Task file: <path to tasks/<slug>.md>
   Handoff doc: <path to latest handoff doc>
   This is handoff #N. Prior handoffs exist but the latest doc is sufficient.
   Read both files before responding.
   ```
2. Claude Code launches in the project's repo directory with the skill `fleet-guard` auto-loaded.
3. Agent reads the task file (permanent context) and handoff doc (session-specific context) and picks up from `## Next Steps`.
4. Agent does NOT receive the progress JSONL or the handoff chain — those are for the human dashboard.

## Open Questions

- **Health metric calibration.** The 50%/70% thresholds are the current design bet (50% validated against Hermes/OpenClaw; 70% is Fleet's own choice for the emergency runway). v1 ships with them, instruments usage, and adjusts in v1.1 based on real data.
- **tmux requirement.** Users who don't have tmux installed get a clear error + install hint. Alternative spawn modes (screen, new terminal window) are deferred.
- **Multi-user future.** `~/.fleet/` is single-user. Shared team dashboards are explicitly out of scope for v1.
- **Cost tracking.** Should Fleet surface token cost per agent? Useful but adds API integration. Defer to v1.1.
- **Self-host dogfood.** Fleet should be built using Fleet once v0.2 boots. Eat your own dog food loudly on Twitter.

## Success Criteria

**Launch (v1.0):**
- Demo gif: 4 agents running concurrently across 4 repos, visible health states, one handoff happening live.
- README with the gif + "Everyone is a manager" thesis.
- Show HN + tweet. Target: 500 GitHub stars in first week, 50 installs confirmed via brew telemetry.

**Month 1 post-launch:**
- ≥20 users report using Fleet daily for multi-Claude workflows.
- ≥1 non-author contributor PR merged.
- Zero corrupted-state crashes reported.

**Month 3:**
- Agent-health-as-primitive gets cited in at least one other AI tooling blog post or comparable project.

## Distribution Plan

- **Binary:** `go build` cross-compiled for darwin-arm64, darwin-amd64, linux-amd64, linux-arm64. GoReleaser for publish.
- **Install:** `brew install edisonshen/tap/fleet` (primary). `curl | sh` script for non-mac. Manual `go install` for devs.
- **Skill:** `fleet-guard` skill lives in the same repo; installable via the standard Claude Code skill mechanism.
- **CI/CD:** GitHub Actions — on tag push, run tests, build binaries, publish release, update homebrew tap formula automatically.

## Dependencies

- **Prerequisite knowledge:** Go TUI ecosystem (bubbletea, lipgloss), Claude Code skills/hooks surface.
- **Prerequisite decisions:** Pick bubbletea vs tview for TUI (recommend bubbletea — richer ecosystem, better aesthetics).
- **Pre-launch:** Need working `fleet-guard` skill proving the 50% context threshold actually helps. This is the one empirical bet worth validating before the month-long build.

## Next Steps

In order (total 7 weeks, realistic 6-8):

1. **Week 0 — Feasibility Spike (GATING):** Write the `fleet-guard` skill in isolation. Answer the three spike questions (hook-exposes-context-pct, latency, proxy-accuracy). Use it yourself for 3-5 days on real sessions. Decision: full-spec, proxy-mode, or pivot. Do not start Week 1 until this is resolved.
2. **Week 1:** Scaffold Go repo at `github.com/edisonshen/fleet`. Basic CLI with cobra: `fleet dispatch`, `fleet attach`, `fleet status`. `fleet dispatch` spawns Claude in tmux and registers an agent. No TUI yet.
3. **Weeks 2-3:** Build the bubbletea TUI reading from `~/.fleet/agents/*.json` via fsnotify. Polling fallback at 1s. Lifecycle probe + stale-agent detection.
4. **Week 4:** Integrate `fleet-guard` skill into the spawn flow. Handoff command + trigger queue + restart-on-handoff flow. Reconciliation logic for orphaned tasks.
5. **Week 5:** Record the demo gif. Write the README. GoReleaser + homebrew tap. Cross-compile for the four targets.
6. **Week 6:** Dogfood for a full week using Fleet to build Fleet. Fix what hurts.
7. **Week 7 (buffer):** Launch. Show HN + tweet thread framed around "Everyone is a manager."

## What I noticed about how you think

- You repeatedly refined the name (task-switchboard → baton → fleet → kept iterating past chief/principal) before writing any code. You cared enough to get the metaphor right because the name carries the thesis. That's product instinct, not engineering instinct.
- You pushed back on my "ship the smallest wedge first" recommendation and picked "All three in one push." You weren't optimizing for minimum-risk; you were optimizing for a strong launch narrative. You'd rather ship once and ship memorable than three times and be forgotten. That's a real call, not a default.
- You picked "Agent health as a first-class metric" as the 12-month bet over three more immediate-feeling options (knowledge transfer, review queues, shared context graph). You chose the most *conceptual* option — the one that reframes how people think about working with AI. That's a framework-builder's pick, not a feature-builder's.
- Your slogan came before your spec: "everyone is a manager" landed, then the product shape reorganized around it. Most people write the README last. You wrote the thesis first.
